spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: http://centos.guguba.top:10911
        file-extension: yaml
        namespace: mall_zf
        ext-config:
          - data-id: gateway.yml
            group: DEFAULT_GROUP
        group: DEFAULT_GROUP